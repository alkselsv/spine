"""Canonical ingestion pipeline.

The existing :mod:`spine.ingest` package remains as a compatibility boundary
for the phase-one file loaders.  New normalization and identity-resolution
work belongs here.
"""
